# Razorpay Configuration
# Get your keys from: https://dashboard.razorpay.com/app/keys

# For Testing: Use test keys (starts with rzp_test_)
# For Production: Use live keys (starts with rzp_live_)

# Public key (safe to expose in client-side code)
# Public key (safe to expose in client-side code)
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_placeholder

# Secret key (NEVER expose this in client-side code)
RAZORPAY_KEY_SECRET=rzp_secret_placeholder

# Instructions:
# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. Sign up at https://razorpay.com
# 3. Go to Settings > API Keys in your dashboard
# 4. Generate Test Keys for testing
# 5. Replace the XXXXXXX values in .env.local with your actual keys
# 6. Restart your dev server: npm run dev
# 7. Test at: http://localhost:3000/book-appointment
